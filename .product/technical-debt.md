# Technical Debt — english-platform

> Auto-generated by /analyze-codebase on 2026-03-12.

## Summary

- **Test files:** 0 (no tests exist)
- **Git history:** 10 commits, single contributor — early-stage project
- **Active migration:** Transitioning from 2-role (admin/student) to 3-role (owner/teacher/student)
- **Deleted admin pages:** Git status shows old `(admin)/` route group files deleted, replaced by `(owner)/` and `(teacher)/`

## In-Progress Work (from git status)

The current branch `fix/next-env-types` has significant uncommitted changes:
- Deleted old admin route group (`app/(admin)/`)
- Added new owner route group (`app/(owner)/`)
- Added new teacher route group (`app/(teacher)/`)
- Added OwnerSidebar and TeacherSidebar components
- Modified auth helpers, proxy, database types, seed data
- Added migration `002_three_roles.sql`

## Missing Test Coverage

No test files exist. Zero test coverage.

## Known Issues

- Old admin references may still exist in components (role migration in progress)
- `proxy.ts` was renamed from middleware for Next.js compatibility
- Some commit messages are in Arabic — mixed language codebase history
